{
  "scripts": {
    "start": "gitbook serve",
    "build": "gitbook install && gitbook build",
    "deploy": "gh-pages -d _book"
  },
  "dependencies": {
    "gh-pages": "^2.1.1",
    "gitbook-plugin-advanced-emoji": "^0.2.2"
  }
}
